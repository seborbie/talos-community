diff --git a/Cargo.toml b/Cargo.toml
index ddd5a0d..794b89e 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -1,20 +1,26 @@
 [package]
+edition = "2018"
+rust-version.workspace = true
 name = "vpx-encode"
-description = "Rust interface to libvpx encoder"
-repository = "https://github.com/astraw/vpx-encode"
-authors = ["Andrew Straw <strawman@astraw.com>", "Ram Kaniyur <quadrupleslap@gmail.com>"]
+version = "0.6.5"
+authors = [
+    "Andrew Straw <strawman@astraw.com>",
+    "Ram Kaniyur <quadrupleslap@gmail.com>",
+]
+description = "Rust interface to libvpx encoder (patched: VP8 cpu_used=2)"
 license = "MIT"
-version = "0.6.2"
-edition = "2018"
+repository = "https://github.com/astraw/vpx-encode"
 
-[dependencies]
-env-libvpx-sys = "5.1.0"
-thiserror = "1.0.33"
+[dependencies.env-libvpx-sys]
+version = "5.1"
+
+[dependencies.thiserror]
+version = "1.0"
 
 [features]
-vp9 = []
 backtrace = []
 ffi-generate = ["env-libvpx-sys/generate"]
+vp9 = []
 
-[package.metadata.docs.rs]
-features = [ "vp9" ]
+[lints]
+workspace = true
diff --git a/README.md b/README.md
index 97a6974..2148ee5 100644
--- a/README.md
+++ b/README.md
@@ -1,28 +1,5 @@
-# vpx-encode
+# vpx-encode (patched)
 
-Rust interface to libvpx encoder
+Rust interface to libvpx encoder. This is a local patch that sets VP8E_SET_CPUUSED=2 (middle preset) for VP8 encoding.
 
-This crate provides a Rust API to use
-[libvpx](https://en.wikipedia.org/wiki/Libvpx) for encoding images.
-
-It it based entirely on code from [srs](https://crates.io/crates/srs).
-Compared to the original `srs`, this code has been simplified for use as a
-library and updated to add support for both the VP8 codec and (optionally)
-the VP9 codec.
-
-## Optional features
-
-Compile with the cargo feature `vp9` to enable support for the VP9 codec.
-
-## Example
-
-An example of using `vpx-encode` can be found in the
-[`record-screen`](https://crates.io/crates/record-screen) program. The source
-code of record screen is
-[here](https://github.com/astraw/vpx-encode/tree/master/record-screen).
-
-## Contributing
-
-All contributions are appreciated.
-
-License: MIT
+Original: https://github.com/astraw/vpx-encode
diff --git a/src/lib.rs b/src/lib.rs
index a2d8fe8..835e28d 100644
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -8,19 +8,7 @@
 //! library and updated to add support for both the VP8 codec and (optionally)
 //! the VP9 codec.
 //!
-//! # Optional features
-//!
-//! Compile with the cargo feature `vp9` to enable support for the VP9 codec.
-//!
-//! # Example
-//!
-//! An example of using `vpx-encode` can be found in the [`record-screen`]()
-//! program. The source code for `record-screen` is in the [vpx-encode git
-//! repository]().
-//!
-//! # Contributing
-//!
-//! All contributions are appreciated.
+//! **Patch:** VP8 encoder sets VP8E_SET_CPUUSED=2 (middle preset) for balanced speed/quality.
 
 // vpx_sys is provided by the `env-libvpx-sys` crate
 
@@ -45,6 +33,9 @@ use vpx_sys::vp8e_enc_control_id::*;
 use vpx_sys::vpx_codec_cx_pkt_kind::VPX_CODEC_CX_FRAME_PKT;
 use vpx_sys::*;
 
+/// VP8 cpu_used preset: 0=slowest/best, 5=fastest. Middle setting (2) balances speed and quality.
+const VP8_CPUUSED: c_int = 2;
+
 #[derive(Copy, Clone, Debug, PartialEq, Eq, Hash)]
 pub enum VideoCodecId {
     VP8,
@@ -124,10 +115,10 @@ impl Encoder {
             VideoCodecId::VP9 => call_vpx_ptr!(vpx_codec_vp9_cx()),
         };
 
-        if config.width % 2 != 0 {
+        if !config.width.is_multiple_of(2) {
             return Err(Error::from("Width must be divisible by 2".to_string()));
         }
-        if config.height % 2 != 0 {
+        if !config.height.is_multiple_of(2) {
             return Err(Error::from("Height must be divisible by 2".to_string()));
         }
 
@@ -141,7 +132,10 @@ impl Encoder {
         c.g_timebase.den = config.timebase[1];
         c.rc_target_bitrate = config.bitrate;
 
-        c.g_threads = 8;
+        // VP8: `vpx_codec_enc_config_default` already picks a safe thread count. Some libvpx
+        // builds (notably Windows vcpkg + CONFIG_REALTIME_ONLY) require
+        // g_threads <= (1 << token_partitions); unconditionally forcing 8 threads makes
+        // vpx_codec_enc_init_ver return VPX_CODEC_INVALID_PARAM (error code 3).
         c.g_error_resilient = VPX_ERROR_RESILIENT_DEFAULT;
 
         let ctx = MaybeUninit::zeroed();
@@ -149,6 +143,8 @@ impl Encoder {
 
         match config.codec {
             VideoCodecId::VP8 => {
+                c.g_threads = 1;
+                let cpu_used = config.vp8_cpu_used.unwrap_or(VP8_CPUUSED);
                 call_vpx!(vpx_codec_enc_init_ver(
                     &mut ctx,
                     i,
@@ -156,9 +152,15 @@ impl Encoder {
                     0,
                     vpx_sys::VPX_ENCODER_ABI_VERSION as i32
                 ));
+                call_vpx!(vpx_codec_control_(
+                    &mut ctx,
+                    vpx_sys::vp8e_enc_control_id::VP8E_SET_CPUUSED as _,
+                    cpu_used,
+                ));
             }
             #[cfg(feature = "vp9")]
             VideoCodecId::VP9 => {
+                c.g_threads = 8;
                 call_vpx!(vpx_codec_enc_init_ver(
                     &mut ctx,
                     i,
@@ -188,7 +190,7 @@ impl Encoder {
         })
     }
 
-    pub fn encode(&mut self, pts: i64, data: &[u8]) -> Result<Packets> {
+    pub fn encode(&mut self, pts: i64, data: &[u8]) -> Result<Packets<'_>> {
         assert!(2 * data.len() >= 3 * self.width * self.height);
 
         let image = MaybeUninit::zeroed();
@@ -268,6 +270,8 @@ pub struct Config {
     pub bitrate: c_uint,
     /// The codec
     pub codec: VideoCodecId,
+    /// VP8 cpu_used: 0=slowest/best, 5=fastest. None = use default (2). Only used for VP8.
+    pub vp8_cpu_used: Option<c_int>,
 }
 
 pub struct Packets<'a> {
@@ -304,7 +308,7 @@ pub struct Finish {
 }
 
 impl Finish {
-    pub fn next(&mut self) -> Result<Option<Frame>> {
+    pub fn next(&mut self) -> Result<Option<Frame<'_>>> {
         let mut tmp = Packets {
             ctx: &mut self.enc.ctx,
             iter: self.iter,
